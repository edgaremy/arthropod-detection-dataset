## PSTL-split dataset requirement

Before using scripts in this folder, you must first build the YOLO dataset in the sibling PSTL folder.

Required first step:

- Build the YOLO dataset in `datasets(others)/PSTL/`

This folder depends on that output and will not work correctly if the PSTL dataset has not been prepared first.
